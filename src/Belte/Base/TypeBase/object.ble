
namespace Base {
    public class object<attribute NotNull>
        where {
            !(NotNull && value == null);
        } {

        object();
        ~object();

        virtual operator==(object obj);
        type GetType();
        virtual string ToString();

    }
}
