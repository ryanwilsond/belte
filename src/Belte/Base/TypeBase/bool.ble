
namespace Base {
    public class bool : IComparable, IComparable<bool>, IConvertible {

        static bool Parse(string str);
        int<-1,1> CompareTo(object obj);
        int<-1,1> CompareTo(bool obj);
        override string ToString();

    }
}
