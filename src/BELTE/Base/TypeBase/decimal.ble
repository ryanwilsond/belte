
namespace Base {
    public class decimal<decimal Min=null, decimal<Min,> Max=null> {
        const decimal MinValue = Min;
        const decimal MaxValue = Max;

        decimal<MinValue, MaxValue> value;

    }
}
