
namespace Base {
    public interface IEquatable<type T> {
    public:
        bool operator==(T obj);
        bool operator!=(T obj);
        bool operator<=(T obj);
        bool operator>=(T obj);
        bool operator<(T obj);
        bool operator>(T obj);
    }
}
