
namespace Base {
    public interface IEquatable<type T> {

        bool operator==(T obj);
        bool operator!=(T obj);
        bool operator<=(T obj);
        bool operator>=(T obj);
        bool operator<(T obj);
        bool operator>(T obj);

    }
}
