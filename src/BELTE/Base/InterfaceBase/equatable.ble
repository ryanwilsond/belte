
namespace Base {
    public interface IEquatable<class T> {
    public:
        bool operator==(T obj);
        bool operator!=(T obj);
        bool operator<=(T obj);
        bool operator>=(T obj);
        bool operator<(T obj);
        bool operator>(T obj);
    }
}
