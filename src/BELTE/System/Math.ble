
namespace System {
    public static class Math {

        int floor(decimal num);
        int ceil(decimal num);
        decimal sin(decimal num);
        decimal tan(decimal num);
        decimal cos(decimal num);
        decimal log(decimal num, decimal base=10);
        decimal pow(decimal base, decimal pow=2);
        decimal sqrt(decimal num);
        decimal root(decimal num, decimal root=2);

    }
}
