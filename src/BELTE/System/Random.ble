
namespace System {
    public static class Random {
    private:

        bool first_ = true;

        void Seed(int<0,> seed);

    public:

        int Randint(int start, int<start,> end);
        decimal Random(decimal start, decimal<start,> end, decimal step=1);

    }
}
