using Buckle;
using Buckle.Building;

void Build(Builder builder) {
    builder.AddInput("src");
    builder.buildMode = .Execute;
    builder.outputKind = .ConsoleApplication;
    builder.debugBuild = true;
    builder.IncludeNETSDK();
    builder.AddRef("libs");
}
