/*

    Hello, world!

    This program writes "Hello, world!" to standard out (the console).

*/

// With no entry point specified, execution starts from the top of the file.
// The `Console.PrintLine` function is included by default and is used to output text to the console.
Console.PrintLine("Hello, world!");
