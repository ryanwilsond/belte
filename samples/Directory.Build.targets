<Project>

    <Target Name="CreateManifestResourceNames" />

    <Target Name="CoreCompile" DependsOnTargets="$(CoreCompileDependsOn)">
        <Exec Command="dotnet run --project &quot;$(MSBuildThisFileDirectory)\..\src\Buckle\Silver\CmdLine\CmdLine.csproj&quot; -- -d @(Compile->'&quot;%(Identity)&quot;', ' ') -o &quot;@(IntermediateAssembly)&quot; @(ReferencePath->'--ref=&quot;%(Identity)&quot;', ' ')"
              WorkingDirectory="$(MSBuildProjectDirectory)" />
    </Target>

</Project>
