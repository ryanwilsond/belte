
void main() {
    string name = getName();
    print("Hello " + name + "!");
}

string getName() {
    string name = readLine("What's your name?");
    return name;
}

string readLine(string message) {
    print(message);
    return input();
}
