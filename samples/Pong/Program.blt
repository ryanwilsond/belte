
static class Program {
    public static void Main() {
        Graphics.Initialize("Pong", Settings.ScreenWidth, Settings.ScreenHeight);
    }

    public static void Update(decimal! deltaTime) {
        Console.PrintLine(deltaTime);
    }
}
