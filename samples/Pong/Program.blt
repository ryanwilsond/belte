
void Main() {
    // Graphics.Initialize("Pong", Settings.ScreenWidth, Settings.ScreenHeight);
    var a = new Sprite();
    var b = new Sprite();
    bool c = Physics.AxisAlignedCollision(ref a, ref b);
}

void Start() {

}

void Update(decimal deltaTime) {

}
